/**
 * Instead of a PCH I have this,
 * Just general things to make compiling easier and faster.
 * (this can be considered a bad practice)
 */
#pragma once

#include <GL/glew.h>
#include <SDL.h>
#include <SDL_opengl.h>
#include <string>
